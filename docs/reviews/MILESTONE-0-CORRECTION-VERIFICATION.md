# Milestone 0 Correction Verification

Last updated: 2026-07-19.

This is the live disposition ledger for every actionable finding in `MILESTONE-0-AND-AUTHORING-RUNWAY-AUDIT.md`. A finding is marked Resolved only when its bounded correction and named evidence pass `./sf verify`. `SF-AUTHORING-000` and audit finding `M0-P1-07` remain outside this correction program.

## Verification baseline

- Branch: `fix/milestone-0-audit-corrections`
- Completed checkpoints: `SF-CORRECTION-001` through `SF-CORRECTION-005`
- Command: `./sf verify`
- Result: passed on 2026-07-19 with 129 unit tests and 14 UI tests, zero failures
- Release/publication actions: none

## Finding disposition

| Finding | Severity | Status | Production change | Tests and evidence | Commit | Remaining limitation |
|---|---:|---|---|---|---|---|
| M0-P0-01 | P0 | Fixed | One typed authorization boundary guards New, Open, Revert, recovery Restore, and Close while preserving a complete lifecycle snapshot. | Exact-state unit tests cover Cancel and failed Save; the native UI journey covers Save/Discard/Cancel, Escape, Return, and save-panel cancellation. | `SF-CORRECTION-001 guard destructive transitions and recover untitled work` | None for this finding. |
| M0-P0-02 | P0 | Fixed | Package bytes, parsed package, bounded digest, byte count, device/inode identity, and security metadata now come from one validated descriptor snapshot; lifecycle open adopts that snapshot directly. | `testValidatedReadSnapshotRemainsBoundToOriginalSourceAcrossReplacementAndSymlinkSwap`; lifecycle conflict preservation test; `./sf verify` with 104 unit + 14 UI tests. | `SF-CORRECTION-002 bind package and recovery I/O to file identity` | None for this finding. |
| M0-P0-03 | P0 | Fixed | The expected digest/size/device/inode is revalidated at commit; exclusive create or `RENAME_SWAP` plus displaced-object verification and rollback prevents a changed destination from being consumed as the expected file. | Deterministic destination replacement, same-inode modification, delete/recreate, and lifecycle exact-state tests preserve external bytes. | `SF-CORRECTION-002 bind package and recovery I/O to file identity` | None for this finding. |
| M0-P0-04 | P0 | Fixed | Recovery remains in app-owned project-ID storage and now requires current-user directory/file ownership, restrictive permissions, project identity, regular single-link identity, and exact candidate fingerprint before replace/delete. | Mismatched/malformed collision, symlink/identity, deletion-failure/retry, untitled round-trip, and cleanup tests preserve unrelated bytes. | `SF-CORRECTION-001 guard destructive transitions and recover untitled work`; `SF-CORRECTION-002 bind package and recovery I/O to file identity` | None for this finding. |
| M0-P1-01 | P1 | Fixed | Every asynchronous lifecycle operation carries a typed epoch, operation ID, document/project identity, revision, destination identity, and intent; every adoption boundary advances the epoch and cancels or drains prior work; all completion paths revalidate the full identity before touching state. | Deterministic Save and executing-autosave crossings with New, Open, Revert, Restore, and Close; stale-success, stale-failure, and two cancellation-boundary tests assert exact state and disk bytes. | `SF-CORRECTION-003 scope lifecycle work by epoch and intent` | None for this finding. |
| M0-P1-02 | P1 | Fixed | Explicit Save serializes against other manual saves and cancels/drains pending or executing autosave before capturing and committing its revision; autosave waits for explicit Save and cannot update its durable binding; edit during Save preserves the captured durable revision and leaves the newer active revision modified/recoverable. | Pending/executing autosave followed by Save, Save As with pending autosave, edit-during-Save, exact write-count, and exact revision-order tests use injected debouncers and actor barriers. | `SF-CORRECTION-003 scope lifecycle work by epoch and intent` | None for this finding. |
| M0-P1-03 | P1 | Fixed | Modified titled and untitled documents write app-owned recovery candidates; launch discovers valid untitled candidates and Restore/Discard are explicit. | `testUntitledRecoverySurvivesRelaunchAndCleansUpAfterRestoreSaveAndDiscard` plus launch discovery integration. | `SF-CORRECTION-001 guard destructive transitions and recover untitled work` | None for this finding. |
| M0-P1-04 | P1 | Fixed | Current schema v2 requires every current document/page field and validates empty/rootless input instead of repairing it; only the schema-v1 adapter migrates legacy defaults. `UInt64.max` is invalid and checked command arithmetic returns typed `revisionExhausted` before mutation at the final commit boundary. | `testCurrentSchemaRequiresEveryCurrentDocumentAndPageField`; `testTerminalRevisionIsRejectedWithoutOverflowOrMutation`; retained invalid-open/adoption tests; `./sf verify` with 119 unit + 14 UI tests. | `SF-CORRECTION-004 enforce strict schemas and migration goldens` | None for this finding. |
| M0-P1-05 | P1 | Fixed | One actor-isolated service owns user-selection authorization, real app-scoped bookmark persistence/resolution/repair, balanced security scopes, coordinated package access, relocation, file presentation, typed failures, and redacted diagnostics. New-save authorization bookmarks and scopes the existing parent before creating the selected child. The unsigned Release candidate declares exactly App Sandbox, user-selected read/write, and app-scoped bookmarks; complete SF-1504 release status is explicitly bounded rather than overstated. | Ten focused tests cover a real Foundation bookmark across service recreation, parent-scoped new-save/relaunch, `0600`/`0700` app storage, stale repair, relocation, scope balance, denial, coordination failure, lifecycle relaunch, presenter events, canonical preservation, declarations, and redaction; `./sf verify` passes with 129 unit + 14 UI tests. | `SF-CORRECTION-005 implement macOS file access boundary` | Owner-approved publisher/trust configuration remains necessary for a signed sandboxed distribution exercise, but is not required to fix the absent implementation/overclaim finding. |
| M0-P1-06 | P1 | Fixed | In addition to the identity-bound no-follow and metadata policy from correction 002, external durable operations now execute inside `NSFileCoordinator` with one balanced security scope obtained only from user selection or a persisted bookmark. `NSFilePresenter` reports move/change/delete without consuming external bytes as canonical state. | Source/destination/ancestor swap, identity, owner/mode/ACL/xattr tests from correction 002 plus correction 005 coordination, scope-lifetime, relocation, external-event, and exact canonical-preservation tests all pass. | `SF-CORRECTION-002 bind package and recovery I/O to file identity`; `SF-CORRECTION-005 implement macOS file access boundary` | None for this finding. |
| M0-P1-07 | P1 | Excluded | None; intentionally outside this program. | Remains queued as `SF-AUTHORING-000`. | — | Architecture-runway evidence remains pending. |
| M0-P1-08 | P1 | Fixed | `OPEN_DECISIONS.md` and the specification now share canonical OD-001/OD-002 meanings; release-only publisher/trust questions are OD-012/OD-013; ADR-0001 explicitly resolves OD-002. | `scripts/check-traceability.py` rejects duplicate/conflicting IDs, absent register rows, and stale evidence. | `SF-CORRECTION-006 reconcile traceability and evidence` | None for this finding. |
| M0-P1-09 | P1 | Fixed | Aggregate status is In progress and every affected requirement is machine-classified as bounded or Partial with explicit uncovered acceptance. | `docs/REQUIREMENT_EVIDENCE.json`; traceability lint; `./sf verify` with 133 unit + 16 UI tests. | `SF-CORRECTION-006 reconcile traceability and evidence` | Full generated-site/authoring acceptance remains accurately unclaimed. |
| M0-P1-10 | P1 | Fixed | Synthetic page/policy timings are explicitly smoke evidence, not renderer/release proof; named-host methodology and prohibited claim boundaries are retained. | `docs/evidence/MILESTONE-0-PERFORMANCE-METHODOLOGY.md`; evidence index marks performance rows Partial. | `SF-CORRECTION-006 reconcile traceability and evidence` | Actual renderer P50/P95, memory, stalls, and frame pacing remain required by SF-AUTHORING-000; no measurement was fabricated. |
| M0-P2-01 | P2 | Fixed | Container parsing, canonical graph validation, and persisted-history decode/validation accept cooperative cancellation checkpoints and preserve CancellationError distinctly. | `testCancellationInsideContainerAndCanonicalValidationIsDistinctAndNonAdopting`; `testCancellationInsideHistoryValidationIsDistinctFromIsolation`. | `SF-CORRECTION-006 reconcile traceability and evidence` | None for this finding. |
| M0-P2-02 | P2 | Fixed | Preview fixtures remain visual-only; new UI journeys feed immutable real package bytes through the production open, validation, Retry, recovery-discovery, Restore, and Discard paths. | `testProductionLoaderOpensRealPackageAndRetriesMalformedBytesWithoutPreviewState`; `testProductionRecoveryDiscoverySupportsKeyboardRestoreAndDiscard`. | `SF-CORRECTION-006 reconcile traceability and evidence` | None for this finding. |
| M0-P2-03 | P2 | Partial | Production state transitions now call an injectable native announcement poster; real recovery proves Return/default action; retained inspection records environment/settings/fixtures and limitations. | `testEveryInteractiveTransitionPostsItsSpecificAccessibilityAnnouncement`; production-loader/recovery UI tests; retained manifest. | `SF-CORRECTION-006 reconcile traceability and evidence` | Complete reverse traversal and retained OS-level VoiceOver speech/actual accessibility-setting exercise remain queued in `SF-CORRECTION-008`. |
| M0-P2-04 | P2 | Fixed | Autosave debounce is injected; a burst produces exactly one recovery write for its latest committed revision and a later edit produces exactly one later write in revision order. Retained lifecycle/history/recovery tests use the same controllable seam instead of wall-clock sleeps. | `testAutosaveBurstCoalescesOnceAndSeparatedEditWritesNextRevisionExactly`; complete race suite; `./sf verify` with 115 unit + 14 UI tests. | `SF-CORRECTION-003 scope lifecycle work by epoch and intent` | None for this finding. |
| M0-P2-05 | P2 | Fixed | Two encoder-independent package-v1/schema-v1 Base64 fixtures retain decoded-package SHA-256 checksums and provenance. The explicit adapter deterministically migrates empty and rootless inputs, preserves legacy identity, adds exact stable minimum identities, rejects malformed legacy variants, reopens deterministic schema-v2 saves, and isolates missing history to a clean baseline. | `testImmutableSchemaOneEmptyGoldenMigratesDeterministicallyAndWithoutHistory`; `testImmutableSchemaOneRootlessGoldenPreservesIdentityAndAddsOnlyMinimumRoot`; `testInvalidSchemaOneGoldenVariantsAreRejectedWithoutCompatibilityDefaults`; fixture README/checksums; `./sf verify` with 119 unit + 14 UI tests. | `SF-CORRECTION-004 enforce strict schemas and migration goldens` | None for this finding. |
| M0-P2-06 | P2 | Pending | None yet. | Assigned to `SF-CORRECTION-007`. | — | Independent lifecycle/session state per window remains required. |
| M0-P2-07 | P2 | Partial | UI scenario injection is compiled only into the app Debug configuration. | Release project configuration has no `DEBUG` compilation condition. | `SF-CORRECTION-001 guard destructive transitions and recover untitled work` | Enforceable headless modules and complete composition checks remain in `SF-CORRECTION-007`. |
| M0-P2-08 | P2 | Pending | None yet. | Assigned after `SF-CORRECTION-007`. | — | Stable PageID-derived accessibility identifiers remain required. |
| M0-P2-09 | P2 | Fixed | Revert, Restore, and Discard now emit lifecycle records with typed operation identity and redacted destination/document tokens on success, failure, or cancellation paths. | `testDiagnosticsCoverRevertRestoreAndDiscardRecoveryWithRedactedIdentity`; retained cancellation/failure lifecycle tests. | `SF-CORRECTION-006 reconcile traceability and evidence` | None for this finding. |
| M0-P2-10 | P2 | Fixed | Fingerprinting is performed incrementally in 64-KiB chunks only after descriptor size validation against the unchanged 8-MiB package limit; before/after descriptor versions must match. | Oversized sparse input is rejected before allocation/hash work; swap and same-inode tests prove identity binding and exact preservation. | `SF-CORRECTION-002 bind package and recovery I/O to file identity` | None for this finding. |
| M0-P2-11 | P2 | Pending | None yet. | Assigned after `SF-CORRECTION-007`. | — | Broader tested secret scanning remains required. |
| M0-P2-12 | P2 | Pending | False scalability claims were removed and the unchanged 8-MiB/4-MiB security bounds are documented. | Performance methodology and requirement index identify the exact missing 500-asset capability. | `SF-CORRECTION-006 reconcile traceability and evidence` | Versioned resource capacity and the representative fixture remain queued in `SF-CORRECTION-008`; no limit was weakened. |
| M0-P2-13 | P2 | Fixed | One JSON evidence index maps affected requirements to exact behavioral tests/artifacts and explicit uncovered boundaries. | `scripts/check-traceability.py` runs from repository checks and rejects unknown IDs, stale paths/symbols, unsupported statuses, and decision conflicts. | `SF-CORRECTION-006 reconcile traceability and evidence` | Product-wide expansion continues as new slices land. |
| M0-P2-14 | P2 | Fixed | Discard validates owned artifact identity, reports typed deletion failure, and retains the complete candidate until unlink and directory synchronization succeed. | `testRecoveryDeletionFailureRetainsCandidateAndRetryDeletesOwnedArtifact` forces real permission denial at a barrier, verifies exact bytes/candidate/failure, restores access, and retries successfully. | `SF-CORRECTION-002 bind package and recovery I/O to file identity` | None for this finding. |
| M0-P3-01 | P3 | Pending | None yet. | Assigned after `SF-CORRECTION-007`. | — | Duplicate/dead open-panel presentation remains. |
| M0-P3-02 | P3 | Partial | UI tests use isolated repository-local recovery directories; `./sf test` and `verify` clean the exact shared fixture root on success, failure, or interruption. | Full `./sf verify` leaves no `.siteforge-test-fixtures` residue. | `SF-CORRECTION-001 guard destructive transitions and recover untitled work` | Fixture construction remains duplicated and must be consolidated after `SF-CORRECTION-007`. |

## SF-CORRECTION-001 proof summary

The correction introduces one typed transition intent and one asynchronous authorization path for every production replacement. Cancel and failed Save compare a full lifecycle snapshot containing canonical document, persisted history snapshot, project identity, URL, durable fingerprint, phase, display name, failure, and recovery candidate. The UI alert is attached above launch/workspace switching so it remains available during New and Open. Save destination selection is injected for deterministic cancellation and failure coverage; the production default remains native `NSSavePanel`.

Recovery packages now live below the app-owned Application Support recovery directory and use stable project identity in the filename. Automated evidence creates only isolated repository-local injected recovery directories; `./sf test` and `./sf verify` clean their exact fixture root even when testing exits unsuccessfully. Relaunch discovery validates candidates off the main actor before presenting Restore or Discard, and successful durable save removes the corresponding app-owned candidate.

## SF-CORRECTION-002 proof summary

Package and recovery I/O now cross one descriptor-based boundary recorded by ADR-0007. A read opens the parent with macOS no-follow-all-components behavior, opens the final regular file relative to that directory without following links, enforces the existing 8-MiB limit before allocating, reads in 64-KiB chunks, and requires matching device, inode, size, modification time, and change time before and after the read. The returned immutable snapshot contains the exact bytes, SHA-256 digest, byte count, identity, owner/group/mode/link count, extended ACL, and approved extended attributes used by package parsing and lifecycle fingerprint state.

Writes create an exclusive `0600` staging file in the opened destination directory, synchronize content and metadata, revalidate destination and ancestor identity, and use exclusive rename for creation or an identity-checked atomic swap for replacement. The displaced expected object is revalidated after the swap; mismatch triggers rollback without consuming external bytes. Durable replacement accepts only current-user, single-link files and preserves owner, group, mode, extended ACL, and the approved xattr allowlist. Recovery directories require current-user ownership and no group/world access; recovery files additionally require restrictive permissions, matching project identity, and exact candidate fingerprint for deletion.

Eleven new tests use actor barriers rather than timing sleeps. They deterministically exercise source replacement, destination replacement, delete/recreate with identical bytes, source/destination/ancestor symlink swaps, same-inode writes before commit and after atomic swap, rollback of a changed displaced object, sparse oversized input, hard-link and foreign-owner policy, real ACL/xattr/mode preservation, recovery collisions, permission-denied deletion and retry, and exact lifecycle/disk preservation. The retained interrupted-write tests cover precommit interruption. Full verification passes with 104 unit tests and 14 UI tests.

## SF-CORRECTION-003 proof summary

ADR-0008 adds a lifecycle-wide epoch above ADR-0007's identity-bound filesystem commit. Each read, durable Save, Save As, autosave, recovery write or deletion, and document transition has an immutable typed identity containing operation ID, epoch, document ID, project ID, revision, sanitized destination identity, and operation intent. New, Open, Revert, Restore, Close, and successful document adoption advance the epoch and invalidate, cancel, or drain old read/save/autosave/recovery work. Success, failure, and cancellation paths compare the complete identity before touching canonical state, URL, project metadata, fingerprint, history, recovery candidate, failure presentation, or lifecycle phase.

Manual Save first cancels and drains pending or executing autosave, then serializes with another explicit save and captures one immutable revision/history/package. Autosave cannot supersede the durable binding or create a false Save conflict. An edit during Save is intentionally not discarded: the captured revision becomes durable, while the active newer revision stays modified and is written as a recovery candidate. Diagnostics include sanitized operation identity and intent without content or complete paths.

Eleven new tests use injected debouncers and actor barriers without race sleeps. They cross Save and executing autosave with every document transition, cover pending/executing autosave immediately followed by Save, Save As, edit-during-Save, stale successful and failed completion, and cancellation both before preparation and immediately before filesystem commit. Exact assertions cover canonical document, project identity, URL, fingerprint, persisted history, recovery candidate, lifecycle phase, external/durable bytes, write counts, and revision ordering. Full verification passes with 115 unit tests and 14 UI tests.

## SF-CORRECTION-004 proof summary

ADR-0009 separates strict current decoding from compatibility migration. Schema v2 requires creation kind, explicit template identity (including JSON null), pages, and every route/role/provenance/root/node page field; canonical validation rejects empty and rootless current pages. Schema v1 alone decodes through a private compatibility DTO and deterministically migrates empty documents or rootless pages before the result crosses the validated package-adoption boundary. Missing or invalid historical fields are corrupt input, and missing persisted history remains a clean isolated compatibility baseline.

Two tracked, encoder-independent package-v1/schema-v1 fixtures cover an empty document and a rootless legacy page. Their decoded-package SHA-256 checksums and fixed source metadata live beside the Base64 bytes. Tests prove exact stable project, document, page, and root identities; approved routes/roles/provenance; deterministic repeated decode and schema-v2 save/reopen; invalid-variant rejection; and clean history isolation.

Canonical validation reserves `UInt64.max`, while the command session refuses a transaction at `UInt64.max - 1` and also uses checked increment arithmetic. Maximum and near-maximum tests prove no trap, one allowed final commit, typed failure afterward, exact document preservation, and unchanged undo/redo stacks. Full verification passes with 119 unit tests and 14 UI tests.

## SF-CORRECTION-005 proof summary

ADR-0010 places one actor-isolated macOS access owner above ADR-0007's identity-bound package store. Initial native-panel selection records a real app-scoped bookmark; recreation resolves it without another panel; stale resolution refreshes old and relocated aliases. Scope acquisition and explicit release enclose the actual `NSFileCoordinator` read/write accessor on success and failure. Application-owned recovery remains outside user-selected scope but retains ADR-0007 ownership and file-identity enforcement.

The lifecycle routes selected Open/Save As authorization and later Open/Revert/Save through this boundary, registers a per-document `NSFilePresenter`, stops presentation on new/close/no-durable recovery, and converts change/deletion into actionable conflict state. Relocation changes only durable URL/display metadata and refreshes persistent access. Tests prove that these events never replace canonical content. Diagnostics expose a one-way resource ID, intent, duration, bookmark/repair flags, result, and failure category, with no content, bookmark bytes, filename, or absolute path.

The Release candidate declares App Sandbox, user-selected read/write, app-scoped bookmarks, and the SiteForge package type while distribution signing stays disabled. Debug/XCTest remains credential-free and unsandboxed for repository-local fixtures. This fixes the missing-boundary and false-verification audit finding without claiming that future external-asset authoring acceptance or a signed distributed configuration was exercised.

## SF-CORRECTION-006 proof summary

The specification and project records now use one decision namespace: OD-001 is the minimum-platform/reference-hardware decision, OD-002 is the persistence representation resolved by ADR-0001/0003/0007, and publisher/distribution questions are OD-012/OD-013. A machine-readable evidence index distinguishes bounded verification from Partial requirements and names every uncovered boundary; repository verification rejects unknown requirements, stale test symbols, contradictory decision IDs, and unsupported statuses.

Package-container parsing, canonical graph validation, and persisted-history validation contain cooperative cancellation checkpoints. Deterministic barrier tests cancel inside real work and prove that cancellation remains distinct from corrupt input and cannot adopt state. Revert, Restore, and Discard diagnostics now record typed, redacted success/failure/cancellation paths.

Two Debug-only composition journeys use immutable repository fixtures but execute the production loader: one covers valid open, malformed failure, path redaction, and Retry; one covers real recovery discovery, keyboard Restore, and Discard. Release builds do not compile the integration seam. Accessibility state changes call an injectable native announcement poster, and a retained visual-inspection manifest names the host, settings, fixtures, observations, automation companions, and limitations. Performance claims are corrected rather than fabricated: current page/policy tests remain smoke checks, while renderer/object/asset percentile, memory, stall, and frame evidence stays required by the authoring runway. Full verification passes with 133 unit tests and 16 UI tests.

## Continuation gate

Milestone 1 feature work must not begin. Continue with `SF-CORRECTION-007`: establish enforceable headless module boundaries and independently owned per-window document sessions. Then complete residual `SF-CORRECTION-008` before `SF-AUTHORING-000` becomes first READY.
