# SF-AUTHORING-004 selection-model evidence

Run `scripts/run-selection-model-evidence.sh` from the repository root to compile the production Foundation-only selection and renderer contracts with Swift 6 `-O`, execute the 100- and 10,000-object fixtures, replace `measurements.json`, and validate its structure.

The retained JSON names the hardware, processor, OS, compiler mode, methodology, raw monotonic-clock samples, nearest-rank P95 values, process resident-memory high-water mark, stable selected-identity digests, and limitations. It measures central command validation/traversal and selection-overlay planning. It is not a claim about final interactive frame pacing, AppKit/Core Animation drawing, production text or image work, transforms, incremental indexing, or owner-approved release budgets.

The running-app UI test `testSelectionEmptySingleMultipleLayersKeyboardAndAccessibilityParity` retains cropped screenshots for [empty selection](empty-selection.png), [single selection](single-selection.png), and [multiple selection](multiple-selection.png). On Mac16,13 / macOS 27.0 build 26A5378n in dark appearance, inspection confirmed stable Layers rows, primary/secondary blue outlines aligned to authored objects, inspector summaries, and status counts without panel overlap. The same journey exercises selected/primary accessibility values, Shift-add, Escape, and next/previous keyboard commands. Actual VoiceOver speech, Reduce Transparency, increased contrast, inactive appearance, and all OS accessibility setting combinations were not manually exercised in this slice; existing material/appearance regression coverage remains green.
